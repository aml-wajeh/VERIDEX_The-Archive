# -----------------------------------------------------------------------------
# Development dependencies for the Full RAG Project.
#
# Install with:
#     pip install -r requirements-dev.txt
#
# The first line pulls in every production dependency, so installing this file
# alone is sufficient for full local development and CI.
# -----------------------------------------------------------------------------

# Include production dependencies first.
-r requirements.txt

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Formatting
black>=24.4.0

# Linting
ruff>=0.5.0

# Notebook support (Pipeline_Demo.ipynb)
ipykernel>=6.29.0
nbformat>=5.10.0
